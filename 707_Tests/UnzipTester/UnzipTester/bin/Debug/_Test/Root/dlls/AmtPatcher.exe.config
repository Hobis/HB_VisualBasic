<?xml version="1.0" encoding="utf-8"?>
<configuration>

  <!-- Runtime Setting -->
  <startup>
    <supportedRuntime version="v2.0.50727"/>
    <supportedRuntime version="v4.0"/>
  </startup>

  <appSettings>
    <!-- 윈도우 타이틀 -->
    <add key="Title" value="프로그램 런처  Ver 1.0"/>

    <!-- 설치 이름 -->
    <add key="InstallName" value="Test-한글 띄어쓰기"/>

    <!-- 설치 여부 (이값에 따라서 설치하기버튼,업데이트버튼이 표시됨) -->
    <add key="InstallYesNo" value="No"/>

    <!-- 업데이트 리퀘스트 Url -->
    <add key="UpdateRequestUrl" value="http://amanta.iptime.org/update/update.xml"/>

    <!-- 업데이트 버전체크 타임스탬프 -->
    <add key="UpdateTimeStamp" value="2016-04-26:18-12:15-991"/>
  </appSettings>

  <!-- App Settings -->
  <!--runtime>
    <assemblyBinding xmlns="urn:schemas-microsoft-com:asm.v1">
      <probing privatePath="dlls"/>
    </assemblyBinding>
  </runtime-->

</configuration>